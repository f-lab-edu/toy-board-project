<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.toyboardproject.mapper.MemberMapper">
    <sql id="memberColumns">
        id,
        login_id,
        user_name,
        password,
        email,
        status,
        created_at,
        updated_at
    </sql>

    <sql id="noIdMemberColumns">
        login_id,
        user_name,
        password,
        email,
        status,
        created_at,
        updated_at
    </sql>

    <select id="getMemberList" resultType="MemberInfoResponse" >
        SELECT
            login_id,
            user_name,
            email,
            status
          FROM member
    </select>

    <insert id="saveMember" parameterType="Member">
        INSERT INTO member(
                <include refid="noIdMemberColumns" />
        ) VALUES (
                #{loginId},
                #{userName},
                #{password},
                #{email},
                #{status},
                NOW(),
                NOW()
        )
    </insert>
</mapper>
